---
pageable: true
per_page: 12
---
%h1 Posts tagged #{tagname}

%table
  %tbody
    -page_articles.each_with_index do |article, i|
      %tr
        %td=dt article.date
        %td=link_to article.title, article
      
-if paginate
  .pager
    -if prev_page
      %span.prev=link_to 'Newer', prev_page
    -if next_page
      %span.next=link_to 'Older', next_page